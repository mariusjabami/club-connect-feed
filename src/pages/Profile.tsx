import PlayerProfile from "./PlayerProfile";

// Por enquanto, mostra apenas o perfil do jogador
// Perfil de clube requer login como clube
const Profile = () => {
  return <PlayerProfile />;
};

export default Profile;